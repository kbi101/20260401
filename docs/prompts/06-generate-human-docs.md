# Role
Act as a Principal Technical Writer and DevOps Engineer for the 'timelord' project.

# Context
We have successfully implemented the feature defined in the capability brief. The code is operational. We now need to translate the rigid technical specifications and the resulting implementation into human-readable, visually rich documentation for non-AI agents (our human Product Managers, API Consumers, and Ops team).

# Task
Ingest the following context:
1. `docs/specs/inbox-intelligence-spec.md`
2. The Java source code generated in the `com.timelord.inbox` package.

Based on this context, generate two distinct Markdown documents tailored for human consumption. You MUST embed standard `mermaid.js` diagrams within these files to illustrate the concepts, and explicitly include markdown image links pointing to the PlantUML SVGs generated by Spring Modulith (e.g., `![Module Canvas](../../target/spring-modulith-docs/module-inbox.svg)`).

If your environment permits, write these directly to the file system. Otherwise, output the raw Markdown blocks.

# Output 1: The User & Integration Guide
**Target Location:** `docs/usage/inbox-intelligence-guide.md`
**Target Audience:** Product Managers, Frontend Developers, and API Consumers.
**Required Content:**
- **Overview:** A plain-English summary of the feature's business value (Job-to-be-Done). Do not mention Java or Modulith here.
- **Visual Architecture:** A Mermaid `sequenceDiagram` showing the exact interaction between the User/Client, the target Modulith module, the Spring Application Events, and the external AI Provider.
- **How it Works:** A simple textual explanation complementing the sequence diagram.
- **Integration Details:** Clear examples of the JSON payloads, API endpoints, or domain events a human developer needs to interact with this feature. Include a sample request/response.
- **Known Limitations:** Human-friendly explanations of the AI guardrails.

# Output 2: The Operations Runbook
**Target Location:** `docs/runbooks/inbox-intelligence-runbook.md`
**Target Audience:** DevOps, SREs, and On-Call Engineers.
**Required Content:**
- **Visual Topology:** A Mermaid `graph TD` (flowchart) mapping the deployment architecture for this feature, highlighting the database, specific S3 buckets/storage, and external API endpoints. Include a `stateDiagram-v2` if the AI capability involves complex state changes.
- **Configuration:** List any environment variables, feature flags, or LLM model pointers required to run this feature in production.
- **Observability:** Document exactly which logs, metrics, or Spring Application Events the Ops team should monitor to ensure the AI is healthy.
- **AI Incident Response:** Step-by-step instructions on what to do if the LLM provider goes down, hits rate limits, or begins hallucinating heavily in production (e.g., exactly which feature flag to toggle to disable this specific capability gracefully).

# Execution
Acknowledge the context ingestion and generate the two complete Markdown files with their respective embedded Mermaid diagrams.